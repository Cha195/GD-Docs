<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/styles.css" rel="stylesheet" type="text/css">
</head>
<body>
    <div class="topnav">
        <div class="title">
            <img style="width: 10%;" src='./gd-doc.png'>
            <h1>GD-Docs</h1>
        </div>
        <div class="title">
            <img style="width: 20%; border-radius: 50%" src="<%= picture %>">
            <a href="/logout">Logout</a>
        </div>
    </div>
    <div class="sidenav">
        <h2 style="padding-left: 1.5em; color: white">Word Documents</h2>
        <form>
            <% if(files.length) { for(var file of files) { %>)
                <form action="/<%= file.id%>">
                    <input class="docButton" type="submit" value=<%= file.name%> />
                </form>
            <%}}%>
        </form>
    </div>
    <div id="main" class="main">
        <% if(success) {%>
            <iframe src="<%= filePath %>"></iframe>
        <%}%>
    </div>
</body>
</html> 
